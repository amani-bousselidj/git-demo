name: CI ass

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: print
      run:  echo "Hello CI/CD from GitHub Actions!"
